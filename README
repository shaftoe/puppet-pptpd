Puppet module to install and manage pptpd on Debian-based platforms

For details have a look at init.pp documentation

Usage example:

include 'pptpd'
pptpd::user { 'myuser': password => 'verysecret' }
